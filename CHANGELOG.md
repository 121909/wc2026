# 变更记录

本文件记录可交付版本和对 Windows 接手开发有影响的变更。日期使用 UTC。

## [Unreleased]

- 预留后续版本的功能、修复和兼容性变更。

## [0.1.0] - 2026-07-16

### 新增

- Windows WPF 被动对局记录器的首版宿主与记录流程。
- 从 `Power.log` 增量读取原始行，并按对局写入 JSONL 证据、事件、快照和摘要。
- 本地会话目录、停止/启动状态和日志缺失时的等待状态。
- [v0.1 记录器说明](docs/07-recording-v0.1.md)、[Windows 接手说明](docs/windows-handoff.md) 和 [记录格式](docs/record-format.md)。
- GitHub Actions Windows restore/build 工作流；当前不运行测试。
- 在 Linux 交付环境使用临时 .NET 8 SDK 完成 Windows 交叉目标 Release 编译，结果为 0 警告、0 错误。

### 边界

- 仅被动记录，不进行鼠标键盘输入、卡牌决策、自动换牌、模型调用或 HSReplay 集成。
- 不修改游戏配置；`log.config` 需要由开发者按接手文档手工配置。
- 不将真实日志、截图、账号信息或密钥提交到仓库。

### 已知限制

- v0.1 依赖客户端输出可解析的 `Power.log`；日志轮转、权限、语言或补丁变化可能造成未知事件或不完整摘要。
- 对手未公开手牌和牌库仍然是未知信息；记录器不会把推断写成事实。
- 目前没有跨机器同步、数据库迁移、日报聚合或在线数据源。
- 尚未在 Windows 实机完成人工验收；请按交接清单逐项记录结果。
